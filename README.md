# Echo Lib

Echo Lib contains the core components needed to implement Echo Chat. 

Note to self: 
- To get this to publish you need to run `git tag -a vX.X.X -m "any name v.X.X.X"` and then `git push --tags` to push the tag to the remote repository.
    This will trigger the GitHub Action to publish the package to NuGet.